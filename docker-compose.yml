version: "3.9"
services:
  backend:
    build: ./backend
    container_name: zcomm-backend
    restart: always
    volumes:
      - ./backend/data:/app/data
      - /etc/letsencrypt/live/zcommapp.com/fullchain.pem:/app/server.crt:ro
      - /etc/letsencrypt/live/zcommapp.com/privkey.pem:/app/server.key:ro
    environment:
      - API_ENV=production
    ports:
      - "8080:8080" # or use 8443 with HTTPS (see instructions)
  frontend:
    build: ./frontend
    container_name: zcomm-frontend
    restart: always

  nginx:
    image: nginx:alpine
    container_name: zcomm-nginx
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt
    volumes_from:
      - frontend:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - backend
      - frontend
